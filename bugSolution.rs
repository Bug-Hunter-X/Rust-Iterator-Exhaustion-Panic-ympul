fn main() {    let mut vec = Vec::new();    vec.push(1);    vec.push(2);    vec.push(3);    let mut iter = vec.iter();    if let Some(x) = iter.next(){        println!("{:?}", x);}    if let Some(x) = iter.next(){        println!("{:?}", x);}    if let Some(x) = iter.next(){        println!("{:?}", x);}    // This will not panic because we check for the end of the iterator.    match iter.next() {        Some(x) => println!("{:?}", x),        None => println!("Iterator is exhausted"),    }} 